# ===========================================================================
# InvertirOnline API credentials
# Copiá este archivo como .env y completá con tus datos reales
# ===========================================================================

# Tu usuario y contraseña de InvertirOnline (la misma que usás para entrar a la plataforma)
IOL_USERNAME=tu_usuario@email.com
IOL_PASSWORD=tu_contraseña

# Umbral de z-score para disparar alertas de arbitraje (default: 2.0)
# |z| >= BOND_ALERT_Z_THRESHOLD => alerta activa
BOND_ALERT_Z_THRESHOLD=2.0

# CORS origins permitidos (separados por coma). En desarrollo usar *
ALLOWED_ORIGINS=*

# ---------------------------------------------------------------------------
# Comisiones IOL (configurables sin tocar código)
# ---------------------------------------------------------------------------

# Costo total round-trip (ambas puntas combinadas, e.g. 0.005 = 0.5% total)
IOL_ROUNDTRIP_COMMISSION=0.005
